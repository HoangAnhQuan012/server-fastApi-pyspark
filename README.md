# server-fastApi-pyspark
# how to run this source
# 1. python -m venv .venv
# 2. .venv/Scripts/activate
# 3. pip install fastapi
# 4. pip install "uvicorn[standard]"
# 5. uvicorn app.main:app --reload